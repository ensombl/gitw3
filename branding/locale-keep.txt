# Locale keys that must KEEP the Forgejo name.
#
# These strings refer to the upstream Forgejo project or to external infrastructure that is
# genuinely named Forgejo. Rebranding them would be actively wrong, not merely cosmetic:
# it would send our users to report our bugs on Forgejo's tracker, or tell them to install a
# "GitW3 runner" that does not exist.
#
# One key per line. Everything not listed here gets the brand substitution.

# Sends bug reports to Forgejo's issue tracker on Codeberg.
# TODO: this should point at our own tracker instead of simply being excluded.
report_message

# About the upstream project itself, and links to its repository.
license_desc

# Checks a TXT DNS record at release.forgejo.org — literally Forgejo's infrastructure.
enable_update_checker_helper_forgejo

# Forgejo's translation project on Codeberg. Contributions go upstream, not to us.
language.localization_project

# Webhook type whose identifier is `forgejo` in config and in the API. The label must match
# the value operators actually write.
settings.web_hook_name_forgejo

# "Forgejo Actions" is the technology name, and the runner is the external `forgejo-runner`
# binary. Telling users to set up a "GitW3 runner" would point them at software that does
# not exist.
settings.actions_desc
runs.no_workflows.help_write_access
runs.no_workflows.help_no_write_access

# Explicitly documented as not displayed in the UI; used by the translation tooling.
test


# ---------------------------------------------------------------------------
# Keys from options/locale_next/*.json
#
# Keys are the same in every language file, so one entry here covers all 72 locales.
# A trailing `*` matches by prefix.
# ---------------------------------------------------------------------------

# A migration source: data coming *from* Forgejo instances. Renaming it would label the
# feature "migrate from GitW3", which is not what it does.
migrate.forgejo.description

# The upstream update checker, linking to Forgejo's own blog.
admin.dashboard.new_version_hint

# The Forgejo Actions integration. "Forgejo Actions" is the technology name and the runner is
# the external `forgejo-runner` binary.
settings.authorized_integration.ui.forgejo_actions_local
settings.authorized_integration.forgejo_actions_local*
actions.runners.runner_details.labels_note
